# delivery.ghalbol.com — HTTP bootstrap for certbot DNS-01 workflow.

server {
    listen 80;
    listen [::]:80;
    server_name delivery.ghalbol.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
