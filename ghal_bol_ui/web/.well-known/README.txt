assetlinks.json — SHA-256 for com.ghalbol (Play-installed app)

Play Console (current UI, 2026):
  Left sidebar → Protected with Play → Play Store distribution
  → Go to Play app signing
  → App signing key certificate → SHA-256

Use the APP SIGNING key fingerprint, not the upload key.

Direct link (pick your app when prompted):
  https://play.google.com/console/developers/app/keymanagement

Paste into assetlinks.json → sha256_cert_fingerprints (colons OK).

Official help:
  https://support.google.com/googleplay/android-developer/answer/9842756
