name: ghal_bol_daemon_client
description: SDK client for the Ghal Bol background daemon (JSON-RPC over Unix socket).
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.11.0

dev_dependencies:
  test: ^1.25.0
